Note that this script will only work with Ubuntu 20.04 and above.
or any version that has /etc/netplan
because older version uses different way to change the IP address.

and note that there are different ways to change the IP configuration for all version of ubuntu.
